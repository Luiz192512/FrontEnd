function mostrarTabela() {
    const img = document.createElement('img');
    img.src = '../img/tabela.png'; // Assuming the image is in the img folder
    img.alt = 'Tabela de Jogos';
    document.body.appendChild(img);
}